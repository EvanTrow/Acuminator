# PX1054
This document describes the PX1054 diagnostic.

## Summary

| Code   | Short Description                                                                               | Type  | Code Fix    | 
| ------ | ----------------------------------------------------------------------------------------------- | ----- | ----------- | 
| PX1054 | A `PXGraph` instance cannot start a long-running operation during the `PXGraph` initialization. | Error | Unavailable |

## Diagnostic Description
A `PXGraph` instance cannot start a long-running operation during the `PXGraph` initialization. The following code elements are considered to be a part of the graph initialization:
- The `PXGraph` and `PXGraphExtension` constructors 
- The `Initialize` method overridden in `PXGraphExtension`
- The `Initialize` method of `PXGraph` that implements the `PX.Data.DependencyInjection.IGraphWithInitialization` interface
- The `Configure` method overridden in `PXGraph` and `PXGraphExtension`

To fix the issue, you remove from the initialization logic the invocation of a long-running operation and rework the related business logic.

## Example of Incorrect Code

```C#
public class SMUserProcess : PXGraph
{
	public SMUserProcess()
	{
		SyncUsers(); // The PX1054 error is displayed for this line.
	}

	private void SyncUsers()
	{
		PXLongOperation.StartOperation(this, () => Console.WriteLine("Synced")); 
	}
}
```

## Related Articles

 - [PXGraph](https://help.acumatica.com/Help?ScreenId=ShowWiki&pageid=198bc715-56b2-37c5-895f-fd136023c404)
 - [PXGraph&lt;TGraph&gt;](https://help.acumatica.com/Help?ScreenId=ShowWiki&pageid=0d056242-513e-13d4-ef6f-06a380a8bc7a)
 - [Asynchronous Execution](https://help.acumatica.com/Help?ScreenId=ShowWiki&pageid=ed8c7ee9-3abb-4406-8e57-274e5d2aacaa)
 - [PXLongOperation Class](https://help.acumatica.com/Help?ScreenId=ShowWiki&pageid=b70e1da2-0376-e933-47c6-45ad18291e6c)